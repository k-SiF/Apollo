#pragma once

namespace apollo {
    enum class Key {
        W, A, S, D,
        L,
        F11,
        Up, Down, Left, Right,
        Space, Esc, 
        Unknown,
        Count
    };
}